# 更新日志

本文件记录面向用户的版本变化。正式发布由 `.github/workflows/release.yml` 在通过测试与构建后更新。

<!-- 新版本由 release.yml 插入到此行之后 -->

## [1.2.0] - 2026-09-09

### 版本亮点

> v1.2.0 聚焦多群定时总结、可信生成进度、灵活模型配置和更透明的软件内更新，让日常自动总结与升级过程都更稳定、更可控。

- **多群定时总结**：每个群聊可以独立设定执行时间、当日或昨日规则及启用状态，多项到期任务会安全串行处理。
- **真实生成进度**：生成任务固定绑定群聊、日期、来源和后端进度，切换表单或页面不会造成显示错位。
- **灵活模型配置**：模型可以自由填写、随时展开选择，连接测试成功后会按 AI Provider 保存在本机。
- **可靠软件更新**：完整展示版本说明与真实下载数据，明确执行 SHA-256 校验，并支持按失败阶段重试。

## 新增

### 多群定时总结

- 原单群定时配置升级为多任务列表，每个群聊可独立设置执行时间、总结当日或昨日消息，以及启用状态；
- 支持添加、编辑、停用、重新启用和带确认的删除，同一群聊最多保留一项定时任务；
- 任务使用稳定群聊 ID 绑定，群名称变化只刷新显示，不会误绑到同名群聊；
- 旧版单群配置会自动、幂等迁移，重复启动不会生成重复任务；
- 多项任务同时到期时按顺序执行，以“群聊 ID + 报告日期”去重，单项失败不会阻断后续任务。

### AI 模型配置

- DeepSeek 与 OpenAI Compatible 均支持直接填写服务商当前提供的任意非空模型标识，不再依赖固定白名单；
- 模型输入框保留自由编辑能力，同时无论是否已有文字都能展开完整选项；
- DeepSeek V4 Flash / Pro 继续作为非限制性内置建议；
- API 连接测试成功后，模型会按 Provider 保存在本机，并在后续下拉列表中直接提供选择；
- 旧 DeepSeek 模型别名和响应模型核验继续兼容，API URL 与 API Key 的现有安全边界不变；
- AI 服务类型与模型控件已对齐，设置页信息层级更清楚。

## 修复

### 真实生成任务与进度

- 手动生成、定时生成和历史重新生成继续共用同一套数据读取、AI 分析、Report Schema、导出及历史归档流程；
- 每次生成固定记录任务 ID、来源、群聊、日期范围、开始时间、状态和真实后端进度；
- 生成期间切换群聊、日期或一级页面时，进度框仍显示正在执行的真实任务，不再被当前表单选择覆盖；
- 百分比只使用后端阶段事件，界面仅通过本地时钟刷新耗时，不模拟任务进度。

### 软件更新可靠性

- Stable、Prerelease 与 Test 使用显式构建通道；远程正式版本更高时正常更新，同一数值版本下 Test / Prerelease 可升级到 Stable，本地数值版本更高时不会降级；
- 下载中断会清理不完整文件并从零重试，校验失败会废弃损坏文件并重新下载，安装程序启动失败会优先复用仍有效的已校验安装包；
- 前端操作保护与后端统一操作锁共同阻止快速重复点击创建并发更新任务；
- Release Notes 的标题、空行、段落、有序和无序列表及基础粗体现在可以稳定显示。

## 优化

### 软件内更新体验

- 更新对话框重新整理为紧凑的状态与进度层，同时保留群聊拾遗现有视觉风格；
- 完整展示 GitHub Release Notes，并显示真实下载百分比、已下载字节和总大小；无法取得总大小时显示不确定进度，不伪造百分比；
- 下载结束后明确进入 SHA-256 文件完整性校验，只有校验通过且用户确认后才启动安装程序；
- 检查、下载、校验和安装程序启动使用独立状态与错误提示，失败原因和下一步更明确。

### 报告与项目展示

- “今日主要话题”中的相关原话署名和日期时间改用与话题正文一致的基础字号；署名继续保持蓝色粗体，日期时间继续使用次级颜色和正常字重；
- HTML 报告与 Playwright 生成的 PNG 共用同一排版规则；
- README 顶部的 Release 与 Downloads 徽章改为同排展示并调整顺序，版本和下载入口更容易识别。

## 下载

| 平台 | 架构 | 文件 |
|------|------|------|
| Windows | x64 | `WeChat-Chat-Summary_1.2.0_x64-setup.exe` |
| Windows | x64 | `WeChat-Chat-Summary_1.2.0_x64-setup.exe.sha256` |

> [!WARNING]
> v1.2.0 的 Windows 安装包仍未进行代码签名，Windows 可能显示 SmartScreen 提示。请只从本项目官方 GitHub Release 下载，并使用同名 `.sha256` 文件核对完整性。

软件内更新仍只访问本项目官方 GitHub Stable Release，精确匹配正式安装包及其 SHA-256 文件。软件不会在启动时或后台自动检查更新，也不会自动降级、静默安装或强制结束进程。

**完整变更日志**：https://github.com/bluntvoice/wechat-chat-summary/compare/v1.1.0...v1.2.0



## [1.1.0] - 2026-09-06

### 版本亮点

新增匿名版报告与历史报告一键重新生成功能。

- 支持在历史中心基于已有正常报告生成匿名派生版本；
- 根据报告日期范围内成员首次真实发言顺序编号为“群友01、群友02……”；
- 在整份报告和多日报告中保持同一成员匿名编号一致；
- 隐藏真实群昵称、微信昵称及成员身份字段，并移除成员发言排行、频次、贡献比例和个性化观察；
- 保留主要话题、讨论脉络、资源及群整体统计；
- 匿名版单独生成带“-匿名版”后缀的 HTML、PNG 和 JSON，不修改正常报告，也不会重新调用 AI；
- 历史中心中间列只显示每个日期的最新正常版，匿名派生版统一在右侧版本区切换；
- 新增正常单日报告“重新生成”，无需返回生成页重新选择群聊和日期；
- 重新生成自动使用原报告的群聊和日期，并采用当前版本生成逻辑及当前 AI 配置；
- 重新生成创建新的最高历史版本，不覆盖旧报告，完成后自动打开最新结果；
- 完善正常版、匿名版、多历史版本、搜索、热力图及已总结状态之间的兼容处理。

匿名版主要用于隐藏群成员身份，不会自动识别或删除聊天正文中自行出现的姓名、联系方式及其他敏感信息，分享前仍需自行确认。



## [1.0.1] - 2026-09-05

### 版本亮点

修复 v1.0.0 中成员昵称与代表发言显示不完整的问题。

- 修复 HTML、PNG 与历史中心报告正文中部分成员昵称未应用蓝色加粗样式的问题；
- 修复成员的群昵称被简写或显示不完整的问题，最终展示优先恢复当前群中的完整群昵称；
- 根据话题证据消息固化代表成员引用，并贯穿 Map、Reduce 与 Final 分析阶段；
- 当模型遗漏具体发言人时，使用证据确认的成员引用补充代表发言，避免整个话题只显示“群友、有人、成员、多人”等泛称；
- 优化唯一昵称和空白标准化匹配，保留同名编号与歧义保护，避免将普通文本误识别为成员；
- 增加长昵称、相似昵称、同名成员、普通词冲突、代表成员传递及真实报告语境回归测试。



## [1.0.0] - 2026-09-05

### 版本亮点

群聊拾遗首个正式稳定版本。

- 支持通过 WeChatDataAnalysis 本地 API 读取微信聊天数据，并提供完整的数据源安装、连接与故障引导；
- 支持单日及最多 7 天连续日期的群聊总结，可逐日生成或合并生成；
- 支持 DeepSeek 与 OpenAI Compatible AI Provider；
- 支持 PNG 长图、HTML 和结构化 JSON 报告导出；
- 提供历史报告中心、全文搜索、群聊活跃热力图及内容屏蔽；
- 提供本机 MCP Server，可供外部 MCP Host 使用受控群聊数据和报告能力；
- 支持定时生成、手动检查更新及 Windows 安装包更新闭环；
- 优化成员昵称识别、讨论脉络、资源标签、报告结构与桌面阅读体验；
- 完善 Windows 安装、覆盖升级、卸载前的软件运行状态检测；
- Windows x64 安装包附带 SHA-256 完整性校验文件。



## [0.2.2] - 2026-08-31

### 版本亮点

- 完成 v0.2.2 当前稳定版本整理。
- 保持群聊拾遗、HTML/PNG/JSON 导出和 Windows 桌面端现有能力。
