# wechat-chat-summary 产品需求文档（PRD）

> 项目：wechat-chat-summary
> 仓库：https://github.com/bluntvoice/wechat-chat-summary
> 文档版本：v1.6
> 状态：基础产品方向已确认，后续随开发持续维护
> 更新日期：2026-09-02

---

## 1. 产品背景

当前项目已经具备较完整的群聊分析链路：通过 `WeChatDataAnalysis` 本地 REST API 获取微信聊天数据，完成本地统计与 AI `map -> reduce -> final` 结构化总结，并输出 JSON、HTML 与 PNG 长图。后续重点是从脚本型能力升级为完整的 Windows 桌面软件。

核心产品闭环：

> **选择群聊与时间 → 生成总结 → 导出长图 → 自动归档 → 历史查看 → 搜索筛选 → 热力图分析。**

---

## 2. 产品定位与目标

### 2.0 当前实施状态

第一开发阶段已在源码中完成可运行的 Windows 桌面端最小闭环：

- Tauri 2 + React/TypeScript 桌面框架；
- 通过 UTF-8 JSONL 桥接复用 Python `group_insight`；
- WeChatDataAnalysis 连接测试、群聊读取与群聊搜索；
- 单日及自定义日期范围；
- DeepSeek / OpenAI Compatible API 设置与连接测试；
- 用户选择独立报告根目录；
- 真实生成 JSON、HTML 和 300 DPI PNG；
- 生成后打开图片、报告数据目录或群聊报告目录；
- 同日重复生成自动保留新版本；
- 原始消息分片只在内存中使用，不写入报告目录。

v0.2.0 已完成以下基础能力：

- 建立向后兼容的统一 `report schema 2.1`，JSON、HTML、PNG 与历史索引共用同一份结构；2.0 历史报告保持只读可用；
- 建立 SQLite 历史数据底座及 FTS 检索索引，按“群聊 → 日期 → 版本”保存报告元数据、模块、统计、资源和导出路径，不复制完整原始聊天正文；
- 启动时安全索引当前导出根目录内的旧报告 JSON，旧文件不被覆盖或改写；
- 重设计手机纵向日报长图：顶部仅保留“群聊拾遗”、`对话名称：YYYYMMDD 总结` 与一句话总结，PNG 与 HTML 暂时使用相同的完整信息模块；
- 从 WeChatDataAnalysis 现有字段提取普通 URL、链接卡片和文件元数据，同主题链接与文件合并归类，无法可靠识别时进入“其他 / 未归类”；
- 群成员名称按“群昵称 → 微信网名 → 微信账号 ID”展示，不使用当前用户设置的联系人备注；同一显示名同时映射到两个及以上不同账号时，视为上游名称碰撞并忽略该显示名，分别回退到微信网名或账号 ID；群关键词在统计层进行规范化去重；疑似微信红包领取页或红包素材链接不进入资源整理；
- AI prompt 增加正式讨论、暂时讨论、闲聊、玩笑、调侃、夸张与反话区分，并通过 `tone`、`confidence` 后处理阻止低可信内容进入结论、行动项和风险；
- 桌面端默认单日选择，记忆上次成功生成的群聊，标记已有历史报告，支持显式保存 API 设置、明确选择 DeepSeek V4 Flash / Pro 并核验 API 实际响应模型、真实阶段进度、无控制台窗口启动，以及分别打开 PNG、HTML 和报告目录。
- 桌面端支持可关闭的每日定时生成：固定一个群聊和时间，仅在软件保持运行时触发，每天最多自动尝试一次；本版本不创建 Windows 后台任务。
- 报告生成后支持人工逐项屏蔽所有内容模块条目，并在本机重新渲染为递增新版本；该流程不重新读取聊天、不再次调用 AI。屏蔽版仅显示所属时间与“已屏蔽，建议在群内查看”，不显示标题、参与者、总结、引用或资源详情，被屏蔽正文不进入新版本 JSON、HTML、PNG 或 FTS 索引；原报告继续保留。
- “轻松插曲”不作为对外报告模块展示；玩笑、调侃和反话的内部识别仍用于严肃内容过滤。
- 日报改为内容优先顺序：今日总览 → 今日速览 → 今日主要话题 → AI 今日观察 → 今日活跃情况 → 报告结尾；不再把统计模块放在主要话题之前，也不再另设重复的“详细讨论脉络”。
- “今日主要话题”按讨论对象、核心问题、上下文指向和回复承接关系合并；时间相邻不能单独作为合并依据。同一话题跨多个不连续时段时仍为一项并保留多个时间区间。
- 结论、行动、问题、风险、引用及资源优先关联进对应主要话题；无法可靠关联的少量内容进入“AI 今日观察”的补充关注，资源无法可靠归类时保留“其他 / 未归类”。

上述桌面体验、内容清洗、完整 PNG、report schema 2.1 与报告结构重排作为 v0.2.1
迭代交付；v0.2.0 保留为历史数据底座和桌面端闭环的基础版本。

v0.2.2 增加成员显示名碰撞防护：当 WeChatDataAnalysis 把同一显示名同时分配给两个及以上
不同账号时，忽略该上游显示名，分别回退到微信网名或账号 ID，且仍不得使用本机联系人备注。

v0.2.3 优化“今日主要话题”的信息密度：以连续讨论脉络为核心，讨论落点、行动事项、开放问题、
风险提示、代表性原话和资源关联作为话题内可选细节；无实际内容时不显示占位字段。Report Schema
升级为 2.2，并继续读取 2.0/2.1 历史报告。

v0.2.2 之后的第四阶段已完成以下能力，并进入 v0.2.3 开发基线：

- 软件自身配置、密钥、SQLite 和任务进度使用 Windows 标准用户数据目录；报告导出目录保持
  独立且新用户默认为空，首次生成前必须选择。旧固定数据目录仅在目标未使用时校验复制并保留源目录。
- SQLite 已建立独立 database schema migration 版本；每日统计按群聊与日期独立保存，不依赖
  报告存在。历史搜索采用 FTS5 与中文子串回退，覆盖群名、总结、Schema 2.2 话题细节、成员和资源。
- 桌面端生成结果使用版本化结构化协议，不再解析 CLI 中文日志；开放问题已纳入玩笑、反话和
  低置信度过滤，旧报告缺少 tone/confidence 时保持兼容。
- 桌面端新增独立历史中心，按“群聊 → 日期/报告 → 版本 → 逻辑模块”组织，支持日期、模块、关键词、
  版本与资源查询，并可打开对应 PNG、HTML 和 JSON。
- “已总结”状态以 SQLite 中是否存在有效报告为唯一事实来源。当前微信群列表按 chat ID 匹配后将
  已总结群置顶并支持“全部 / 已总结”筛选；新生成、定时生成或切换导出目录重新导入后立即刷新状态。

本阶段已提供可在本机及 GitHub Windows Runner 复用的 Windows 安装包构建脚本：将 Python
核心打包为安装目录内的独立分析引擎，安装后不依赖源码目录；安装向导允许选择程序目录，
升级和卸载不得触碰用户报告目录。GitHub Actions 可人工生成测试安装包，或在全部测试与构建
通过后人工确认创建 Stable/Prerelease；建立工作流本身不代表已经发布新版本，也不等同于代码签名。

第五阶段在 v0.2.3 开发分支完成群聊日历热力图：用户只对主动选择的群聊和最多 366 天范围
按需补齐统计；连续缺口按最多 31 天分段读取，过程只做本地聚合、不调用 AI，并且仅把消息数、
有效消息数、参与人数、有效字符数、链接数和文件数写入 `chat_daily_stats`。真实 0 与尚未统计
在数据协议和视觉上保持区分，读取失败不得写成 0。页面默认最近一年，支持年份、自定义范围和
三项指标切换；已有报告的日期可以跳转历史中心。

第六阶段当前已完成 AI Provider 结构整理、独立设置页和本机 MCP Server：软件自身生成继续使用
DeepSeek / OpenAI Compatible API；MCP Server 则由外部 MCP Host 调用，二者共享数据读取、统计、
Report Schema 2.2、渲染和历史能力，但调用链互不依赖。本阶段没有实现 MCP Client。

MCP Server 默认关闭，仅监听 `127.0.0.1` 的 Streamable HTTP；由桌面程序托管子进程，软件退出时
同步终止。外部 AI 提交的新报告必须先通过严格 Schema 2.2 校验，再由软件分配版本、生成 JSON / HTML /
PNG、写入 HistoryStore 并刷新已总结缓存。第六阶段已合并并通过人工验收；本软件仍未实现 MCP Client。

第七阶段已在源码中完成独立关于页和用户手动触发的正式更新闭环。软件仅在用户点击“检查更新”后读取
官方 GitHub 最新 Stable Release，使用 SemVer 比较版本并精确匹配 Windows x64 正式安装包及其
`.sha256` 文件；安装包下载到系统临时目录，按真实字节显示进度，SHA-256 一致且用户再次确认后才启动
安装程序。只有安装程序成功启动，当前软件才退出。该能力不包含启动检查、后台检查、自动降级、测试版
通道或代码签名。

### 2.1 产品定位

`wechat-chat-summary` 最终以 **Windows 桌面软件** 为主要产品形态。软件以本地微信数据为基础，通过用户配置的 AI API，或通过 MCP 向外部 AI 客户端提供群聊分析能力，生成适合长期保存和微信群分享的结构化总结与高质量长图。

### 2.2 核心价值

1. **快速回顾**：提取主要内容、热点和成员互动。
2. **便于分享**：生成适合手机纵向阅读和微信群传播的总结长图。
3. **长期沉淀**：按群聊保存历史总结，支持日期筛选、模块筛选和搜索。
4. **趋势观察**：通过日历热力图观察群聊长期活跃情况。
5. **资源整理**：自动汇总当日群聊中出现的链接和文件，并按讨论主题归类，同一主题资源集中展示。
6. **本地优先**：原始微信数据库由本地数据源管理，软件不额外复制保存完整原始聊天正文。

### 2.3 V1 核心范围

- Windows 桌面 GUI；
- 群聊与日期选择；
- 单日 / 自定义时间范围总结；
- PNG / HTML / JSON 导出；
- 用户自定义导出目录；
- 历史中心；
- 日期筛选；
- 总结模块筛选；
- 历史总结搜索；
- 群聊日历热力图；
- AI API 模式；
- MCP Server 模式；
- 当日链接 / 文件按主题整理；
- 关于页面与检查更新。

### 2.4 当前不作为 V1 核心目标

- 群聊多维 BI 仪表盘；
- 软件内置 MCP Client；
- 超出历史总结范围的原始聊天复杂全文检索分析。

---

## 3. 产品信息架构

桌面端至少包含：

1. **首页**
2. **生成总结**
3. **历史中心**
4. **热力图分析**
5. **设置**
6. **关于**

### 3.1 首页

展示快速生成入口、最近生成的总结、最近使用的群聊、数据源状态、当前 AI 分析模式，以及历史中心与热力图入口。

### 3.2 生成总结页

支持选择群聊、选择单日或自定义时间范围、查看当前 AI 模式、启动分析、查看生成状态、预览长图，以及打开图片、打开所在文件夹、复制路径、进入对应历史记录。

### 3.3 历史中心页

历史中心必须为独立页面，并以群聊为一级对象组织数据。至少支持群聊列表、历史总结列表、日期筛选、总结模块筛选、搜索、详情、历史版本和打开对应 PNG / HTML / JSON。

### 3.4 热力图分析页

支持选择群聊和时间范围，展示 GitHub 风格日历热力图。默认以消息数量作为深浅依据，并支持切换其他统计指标。点击某天可查看统计或跳转对应历史总结。

### 3.5 设置页

至少包含 WeChatDataAnalysis 本地 API、AI 分析模式、API 参数、MCP Server、导出根目录、历史数据存储、PNG 导出与高级调试选项。

### 3.6 关于页

关于页面参考 In Line 项目的信息组织和检查更新交互，但保持本项目自身视觉风格。

至少展示：

- 软件名称；
- 当前版本号；
- GitHub 项目地址：`https://github.com/bluntvoice/wechat-chat-summary`；
- 复制项目地址；
- 检查更新；
- 必要的项目说明、隐私及许可状态说明。

“检查更新”和“复制项目地址”等辅助按钮保持一致的尺寸、字号、圆角、边框、背景与交互状态，不应成为页面主要视觉焦点。

---

## 4. 数据源

- 微信消息统一通过 `WeChatDataAnalysis` 本地 API 读取；
- 默认仅连接本机地址；
- GUI 显示连接状态并支持手动测试；
- 数据源不可用时 fail-fast；
- 不恢复旧 `wechat-decrypt` 运行依赖；
- 从数据源读取可用群聊，并支持群聊名称搜索；
- 群聊不存在或时间范围无可用数据时给出明确提示。

---

## 5. AI 分析

### 5.1 分析模式

软件提供两条彼此独立的调用链：桌面端“生成总结”使用用户配置的 **AI API**；同时可选开启
**MCP Server**，供外部 MCP Host 读取受控上下文并提交分析结果。MCP Server 不是 AI Provider，
本软件当前也不是 MCP Client，不把两者设计为二选一的全局分析模式。

### 5.2 API 模式

支持 DeepSeek 与通用 OpenAI Compatible API。通用 Chat Completions、JSON 响应、重试、超时、
模型和 URL 由公共客户端实现；DeepSeek 单独扩展 Thinking、Reasoning Effort、余额接口和模型校验，
通用 Provider 请求不得携带这些 DeepSeek 专属字段。至少可配置：

- API Base URL；
- API Key；
- Model；
- 必要的高级参数。

要求支持连接测试，Key 仅保存在本机私有配置，配置缺失时不得静默使用其他来源，并保持 AI provider 与数据源解耦。

### 5.3 MCP 模式

软件作为 **MCP Server**，默认关闭，仅在用户主动启用时开放。

最小能力：

- 列出可用群聊；
- 获取指定群聊、指定时间范围的统计数据；
- 获取用于分析的结构化消息上下文；
- 获取历史总结；
- 获取指定历史总结模块；
- 获取指定时间范围内的链接与文件资源及其上下文；
- 接收符合软件报告结构的 AI 总结结果用于渲染、导出和归档；
- 在明确授权下触发报告生成或渲染。

MCP 默认仅本机使用并遵循最小必要暴露原则。当前使用 Streamable HTTP，固定绑定 `127.0.0.1`；
原始消息只允许按明确 `chat_id + start + end` 临时读取，单次最多 31 天、2000 条消息和 100 万字符，
不写入 SQLite 或报告目录。报告写入不接受任意路径，只走已配置的受控报告根目录和版本分配逻辑。

---

## 6. 总结生成与长图

标准流程：

1. 选择群聊；
2. 选择日期或时间范围；
3. 获取聊天数据；
4. 完成本地统计；
5. 提取并整理链接、文件；
6. 完成 AI 分析；
7. 生成标准化报告结构；
8. 渲染 HTML；
9. 生成 PNG；
10. 写入 JSON；
11. 写入历史中心索引。

### 6.1 长图核心模块

日报固定使用以下一级顺序：

1. **今日总览**：`群聊拾遗`、`对话名称：YYYYMMDD 总结`、一句话总结及确定性核心统计；
2. **今日速览**：约 3–5 个动态摘要卡片，不强制凑数；
3. **今日主要话题**：按语义话题组织完整讨论脉络，同一话题可有多个时间区间；
4. **AI 今日观察**：不重复话题摘要，不推测成员性格或真实意图；
5. **今日活跃情况**：成员观察、发言排行、群关键词和活跃时段；
6. **报告结尾**：简短结语、报告日期及生成说明。

“今日主要话题”每项以连续、紧凑的讨论脉络为核心，不再重复拆出“关键观点”和“讨论转折”。标题在上，
时间范围在下并使用普通正文样式，不使用胶囊外框；话题正文中的群成员昵称使用蓝色加粗。明确形成结论、
共识、决定、安排或可复述落点时，才显示“讨论落点”；行动事项、开放问题、风险提示、代表性原话及
链接/文件资源均为话题内可选细节。无法可靠关联的资源进入“其他 / 未归类”，没有实际内容的字段不显示
占位文案或空白大卡片。

“今日有趣内容 / 轻松插曲”不作为对外报告模块；其内部分类仅用于防止玩笑、调侃或反话被误写为正式事实。最早消息和最晚消息不在报告中展示。

当前阶段 PNG 不启用自动精简，应与 HTML 保持相同的信息量和完整资源清单。后续如需重新引入摘要长图或模块选择，必须先确认产品行为。

### 6.2 人工屏蔽

报告生成后允许用户在桌面端按模块查看条目并逐项屏蔽。屏蔽属于本地报告编辑，不触发 AI
重新总结。每次提交屏蔽均生成递增新版本并保留原版本。屏蔽占位卡仅展示条目所属时间和
“已屏蔽，建议在群内查看”；不得展示标题、参与者、总结、引用、资源标题、URL 或上下文。
PNG、HTML、JSON、SQLite 历史与搜索索引必须保持一致。无独立时间字段的条目使用报告日期
或统计区间作为所属时间。

### 6.3 长图视觉要求

- 纵向长图；
- 适合手机直接阅读；
- 浅色背景、白色卡片、少量柔和强调色；
- 视觉语言参考 `wzj042/wechat-auto-insight`：页面采用米白至浅绿渐变，头图采用柔和绿—黄渐变，讨论脉络使用圆形序号与右侧正文的紧凑结构；
- 信息密度高但不过度拥挤；
- 不应像后台仪表盘截图；
- 不应只是纯文本报告截图；
- 核心内容无需打开 HTML 即可理解；
- 异常长文本、URL 或文件名不得破坏整体布局。

### 6.4 300 DPI

最终导出的 PNG 必须写入 **300 DPI** 对应的图片元数据：

- 300 DPI 为默认值和验收基准；
- 不得通过低质量二次缩放伪造；
- 实际像素尺寸与渲染清晰度应足以支持手机查看和常规打印；
- 若未来允许调整 DPI，300 DPI 仍应作为默认设置。

---

## 7. 当日链接与文件整理

### 7.1 目标

统一整理指定总结时间范围内出现的链接、链接卡片和文件，避免用户再次在长聊天记录中查找。

### 7.2 资源类型

至少识别：

- 普通 URL；
- 微信链接卡片 / `appmsg` 中可解析的链接；
- 文件消息及可解析的文件元数据。

微信红包消息、疑似红包领取页以及红包素材链接不属于可回顾资源，应在资源提取阶段忽略。

本功能主要整理消息已有资源信息和元数据，不要求自动下载、复制或重新托管原文件。

### 7.3 主题归类

- 结合消息上下文、当日热点和讨论主题归类；
- 存在多个明显主题时分别建立分组；
- **相同主题的链接和文件必须放在同一组中展示**；
- 无法可靠判断主题时进入“其他 / 未归类”，不得强行编造主题；
- 主题名称应简短、可读，并尽量与当日主要讨论主题保持一致。

单条资源在数据可获得时至少保留：资源类型、标题或文件名、URL、发送者、发送时间、所属主题、必要的上下文摘要。

### 7.4 历史与搜索

“资源整理”作为历史总结的独立可筛选模块。搜索应能命中资源标题、文件名、URL、主题名称及上下文摘要；历史详情按主题展示资源；链接类资源可通过系统默认浏览器打开。

---

## 8. 导出与文件管理

### 8.1 独立导出目录

用户必须单独选择导出根目录。真实报告不得默认写入软件安装目录、源码目录、程序运行目录或 WeChatDataAnalysis 安装目录。

首次导出如未配置目录，应要求用户选择后再继续。

### 8.2 目录结构

按群聊建立目录，导出图和报告分析数据分开保存。PNG 按年月分层，报告数据按
日期及版本建立独立目录：

```text
<导出根目录>\
  <群聊名称>\
    导出图\
      YYYY\
        MM\
          YYYY-MM-DD报告.png
    报告数据\
      YYYY-MM-DD报告数据\
        <群聊名称>_YYYY-MM-DD_群聊总结.html
        <群聊名称>_YYYY-MM-DD_群聊总结.json
        map、reduce、final 等派生分析结果与统计缓存
```

群聊名称用于文件系统路径时需要清理非法字符，但界面展示真实群聊名。
多日总结使用 `YYYY-MM-DD_至_YYYY-MM-DD`。跨月报告的 PNG 归入开始日期对应的
年份与月份目录。

### 8.3 文件命名

文件名必须体现日期，格式固定为 `YYYY-MM-DD`。PNG 位于所属群聊目录内，使用：

`YYYY-MM-DD报告.png`

HTML / JSON 使用：

`<群聊名称>_YYYY-MM-DD_群聊总结.<ext>`

### 8.4 同日历史版本

同一群聊、同一日期重复生成时不覆盖旧版本，例如：

```text
2026-08-30报告.png
2026-08-30报告_v2.png
2026-08-30报告_v3.png
```

对应报告数据目录依次为 `2026-08-30报告数据`、`2026-08-30报告数据_v2`、
`2026-08-30报告数据_v3`。历史中心默认展示最新版，并允许查看旧版本。

---

## 9. 历史中心与搜索

### 9.1 保存内容

保存：

- 总结结果；
- 本地统计数据；
- 报告元数据；
- 总结模块结构；
- 当日链接 / 文件资源整理结果；
- PNG / HTML / JSON 文件路径；
- 生成时间；
- 报告日期；
- 版本；
- AI provider / model 等必要元数据。

软件**不额外复制保存完整原始微信聊天正文**。

### 9.2 组织方式

按“群聊 → 日期 → 版本”组织。

群聊列表保留 SQLite 中全部历史群，即使该群已不在当前 WeChatDataAnalysis 群列表中；生成总结页
只显示当前数据源返回的群。报告列表默认显示同群、同分析时间范围的最新版本，详情可切换全部旧版本。

### 9.3 日期筛选

至少支持指定日期、最近 7 天、最近 30 天、本月、自定义日期范围。

### 9.4 模块筛选

历史 UI 使用以下逻辑模块：全部、主要话题、AI 今日观察、成员 / 活跃情况、讨论结论、行动事项、
开放问题、风险提示、代表性原话、资源。

这些模块由查询层从 Report Schema 2.2 派生，不修改报告主结构：主要话题对应 `content.topics[*]`
的标题与 `discussion_flow`；讨论结论、行动事项、开放问题、风险提示和代表性原话分别对应话题内的
`outcome`、`action_items`、`open_questions`、`risk_flags`、`quotes`；资源结合
`topics[*].resource_ids` 与 `resources`。派生时避免把已拆出的嵌套细节再次放入主要话题模块。
2.0/2.1 报告继续通过兼容层读取。

### 9.5 搜索范围

搜索历史总结及派生数据，不提供原始微信聊天全文搜索。至少搜索：群聊名称、一句话总结、主要话题
标题及讨论脉络、讨论结论、行动事项、开放问题、风险提示、代表性原话、成员观察，以及资源标题、
URL、文件名和上下文摘要。

每条搜索结果展示群聊、日期、命中模块、命中摘要与版本，点击后进入对应历史详情并尽量定位命中模块。

### 9.6 存储建议

使用 SQLite 管理桌面端历史数据，采用 FTS5 建立全文索引，并始终保留中文子串回退。

如果用户手动移动或删除导出文件，历史记录不得导致软件崩溃，应提示“文件已移动或不存在”，并可提供重新定位、重新导出或移除失效记录等处理方式。

---

## 10. 热力图分析

采用 **GitHub 风格日历热力图**，每个格子对应一天。

- 默认指标：**消息数量**；
- 允许切换：消息数量、活跃人数、有效消息数量；
- 支持选择群聊；
- 支持选择年份或时间范围；
- 悬停显示当天统计；
- 点击某天查看数据；
- 当天有历史总结时可直接进入对应总结。

热力图统计可基于历史统计结果及数据源按需补齐，不要求为热力图复制完整原始聊天正文。

SQLite 无对应日期记录表示“尚未统计”，不能直接解释为 0。用户打开群聊和范围后，Python 查询
`chat_daily_stats`、合并连续缺口并按最多 31 天分段从 WeChatDataAnalysis 读取消息，只执行
本地统计并批量回写。成功读取但没有消息的日期写入明确零值；读取或写入失败的日期继续保持未知。
单次范围最多 366 天，不得在软件启动时扫描所有群聊或全部历史年份。

---

## 11. 关于与检查更新

### 11.1 关于页面

至少包含：软件名称、当前版本号（建议 `vX.Y.Z`）、GitHub 项目地址、复制地址、检查更新、必要的项目说明与隐私 / 许可状态说明。

### 11.2 检查更新方式

第一版采用**手动检查更新**，参考 In Line：

- 仅在用户点击“检查更新”后访问 GitHub；
- 软件启动时不得自动检查；
- 不做后台周期检查；
- 检查失败不得影响其他功能。

### 11.3 更新来源与版本判断

- 仅使用本项目官方 GitHub Releases；
- 普通通道仅使用正式 Release；
- 不使用 Draft；
- 不使用 Prerelease / Beta / RC 作为普通更新来源；
- 远程版本高于本地版本时才提示更新；
- 按语义化版本比较，不得简单字符串比较；
- 不提供自动降级。

### 11.4 更新状态

至少提供：`检查更新`、`正在检查…`、`已是最新版本`、`发现新版本 vX.Y.Z`、`检查更新失败，请稍后重试`。检查期间禁止重复触发。

### 11.5 下载安装流程

桌面端使用以下更新下载与安装流程：

1. 从官方 Release 选择明确适用于当前 Windows 架构与安装方式的安装包；
2. 下载到系统临时目录，不写入导出目录、数据目录或软件安装目录；
3. 展示真实下载进度，无法获得总大小时不得伪造百分比；
4. 下载同名 `.sha256` Release Asset，并与本地计算的 SHA-256 比较；
5. 仅校验通过后启动正式安装程序；
6. 安装程序成功启动后当前软件才允许退出；
7. 任何更新失败均不得影响软件正常使用。

该机制属于“官方 Release 来源限制 + 完整性校验”，不得描述为独立数字签名或绝对安全机制。

正式资产命名固定为 `WeChat-Chat-Summary_<version>_x64-setup.exe` 及同名
`.sha256`；普通更新通道不选择测试包、portable、sidecar 或其他 `.exe`。安装器只递归替换和卸载
安装根目录内的 `program`，不得删除 Windows App Local Data、SQLite、MCP/API 设置、热力图缓存、
安装根目录中的其他用户文件或用户选择的报告目录。

---

## 12. 隐私与安全

### 12.1 本地保存

- 原始微信数据库不由本软件另行托管；
- `.env`、API Key、真实聊天快照和报告不得提交到 Git；
- 历史索引和导出文件保存在用户本地；
- 导出目录由用户自主选择。
- map 阶段原始消息输入不得持久化；允许保存统计、结构化总结、阶段派生结果与
  不含完整原始正文的缓存元数据。

### 12.2 API 模式提示

软件必须明确提示：

> “本地保存”不等于“聊天文本不会离开本机”。当使用云端 AI API 时，为完成分析所需的聊天文本会发送给用户配置的 AI 服务提供商。

该提示至少出现在首次配置 API、设置页 AI 区域和隐私说明中。

### 12.3 MCP

MCP Server 默认关闭，用户主动启用后才提供服务，默认仅绑定本机，工具能力遵循最小必要原则。

---

## 13. UI / UX 原则

- 按钮不要过大；
- 不使用无意义分割线；
- 保持适度留白，避免页面空荡；
- 信息层级清晰；
- 主要操作突出，辅助操作弱化；
- 历史中心和生成页面以效率优先；
- 关于页面的“复制地址”“检查更新”等采用一致的轻量按钮样式。

---

## 14. 架构与开发原则

- GUI 与分析核心解耦；
- 数据源与 AI provider 解耦；
- 历史中心、搜索、热力图、资源整理模块化；
- 不因桌面端开发破坏现有 CLI 核心能力；
- Windows 文件读写统一使用 UTF-8；
- 关键配置缺失时 fail-fast；
- 真实输出目录不得放在源码仓库或软件安装目录。

---

## 15. V1.0 验收标准

必须满足：

- GUI 可完成群聊与日期选择；
- 正常生成结构化总结；
- 生成 PNG、HTML、JSON；
- 导出目录由用户选择；
- 文件不得默认写入程序目录；
- 文件名必须包含 `YYYY-MM-DD`；
- 同日重复生成保留历史版本；
- PNG 默认正确写入 **300 DPI** 元数据，并具备足够实际像素清晰度；
- 生成结果进入历史中心；
- 历史中心可按群聊、日期和模块查看；
- 可以搜索历史总结和资源信息；
- 可以查看 GitHub 风格群聊日历热力图；
- 热力图默认按消息数并支持指标切换；
- 设置页可配置 AI Provider，并独立启停本机 MCP Server；
- 当日链接与文件可以按主题归类，同主题资源集中展示；
- 关于页显示版本号、项目地址和检查更新入口；
- 软件启动时不会自动联网检查更新；
- 检查更新失败不影响其他功能。

---

## 16. 发布与许可风险

当前项目参考、使用或改造了其他公开项目能力，部分上游许可状态需要谨慎处理。因此：

- 未取得明确依据前，不得擅自声明整个项目采用某一开源许可证；
- 公开发布 Windows 安装包前应核查依赖、子模块与上游代码的许可证 / 授权边界；
- 关于页、README 与 Release 的许可说明必须与实际授权状态一致；
- 不得把“公开可见”视为当然具有复制、修改或再分发授权。

---

## 17. PRD 维护规则

本文件属于项目长期需求基线。

### 17.1 开发前

Codex / 开发者必须先阅读 `PRD.md`、`AGENTS.md` 与当前 README，不得只根据单次聊天指令忽略已确认 PRD。

### 17.2 存在疑问时

如果需求、产品行为或技术实现边界存在歧义：

> **不得擅自替用户决定。必须先以选项形式说明差异、影响及建议方案，等待明确确认后再实施。**

### 17.3 完成开发后

每次开发完成必须同步检查：

- PRD 功能状态是否需要更新；
- README 是否需要同步；
- `.env.example` 是否需要同步；
- `AGENTS.md` 是否需要增加长期开发约束；
- 测试、发布文档是否需要同步。

如果实现结果与 PRD 不一致，应优先确认需求，而不是静默修改 PRD 迁就实现。

---

## 18. 基础测试要求

修改现有分析核心后至少运行：

```powershell
python -m unittest discover -s tests -v
python -m compileall -q group_insight tests
git diff --check
```

引入桌面端后，应补充对应的前端 / 桌面端构建、类型检查与测试命令，并写入 `AGENTS.md`。
